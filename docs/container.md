
* have a separate compose file for built jupyter lab build saved state
  * no args in this one
  
# TODO:

* build .Renviron from environment variables in docker
* fix `jupyter lab build` to run from Dockerfile
* install plotly-orca and have it run in Docker container
